name: SEO Autopilot Legacy

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled. SEO decisions are generated by ChatGPT using GSC Wizard and applied through apply-seo-actions.yml."
